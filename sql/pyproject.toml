[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "fip-sql"
version = "0.1.0"
requires-python = ">=3.13,<3.14"
dependencies = [
    "dbt-postgres==1.10.0",
]

[tool.uv]
package = false
