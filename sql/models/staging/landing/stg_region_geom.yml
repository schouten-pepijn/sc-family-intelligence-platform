version: 2

models:
  - name: stg_region_geom
    description: "Landing region geometries normalized to PostGIS geometry in SRID 4326."
    columns:
      - name: source_name
        tests: [not_null]
      - name: natural_key
        tests: [not_null]
      - name: retrieved_at
        tests: [not_null]
      - name: run_id
        tests: [not_null]
      - name: schema_version
        tests: [not_null]
      - name: http_status
        tests: [not_null]
      - name: region_id
        tests: [not_null]
      - name: region_level
        tests: [not_null]
      - name: region_name
        tests: [not_null]
      - name: source_version
        tests: [not_null]
      - name: valid_from
        tests: [not_null]
      - name: geom
        tests: [not_null]
        description: "PostGIS geometry in 4326"
