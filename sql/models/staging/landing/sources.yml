version: 2

sources:
  - name: landing
    schema: landing
    tables:
      - name: cbs_observations
        description: Landing table loaded by the Python export.
        columns:
          - name: source_name
            tests:
              - not_null
          - name: natural_key
            tests:
              - not_null
          - name: retrieved_at
            tests:
              - not_null
          - name: run_id
            tests:
              - not_null
          - name: schema_version
            tests:
              - not_null
          - name: http_status
            tests:
              - not_null
          - name: observation_id
            tests:
              - not_null
          - name: measure_code
            tests:
              - not_null
          - name: period_code
            tests:
              - not_null
          - name: region_code
            tests:
              - not_null
